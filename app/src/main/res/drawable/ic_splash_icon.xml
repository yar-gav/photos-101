<?xml version="1.0" encoding="utf-8"?>
<!-- Larger inset (36dp) so the full icon fits inside the splash screen circle. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:left="48dp"
        android:top="48dp"
        android:right="48dp"
        android:bottom="48dp">
        <bitmap
            android:src="@drawable/ic_photos_gallery"
            android:gravity="fill" />
    </item>
</layer-list>
